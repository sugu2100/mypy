# 성적의 총점과 평균
# 변수 선언 
math_score = 80
eng_score = 85

# 합계
total_score = math_score + eng_score

# 평균
avg_score = total_score / 2

#출력
#print("합계 :", total_score)
# 숫자와 문자가 함께 사용될 때 문자로 형 변환됨 (문자 > 숫자)
print("합계 : " + str(total_score))

#print("평균 :", avg_score)
print("평균 : " + str(avg_score))
